# Azure VM Configuration Template
# Copy this file to .env and modify with your values

# Resource Group and Location
RESOURCE_GROUP=myResourceGroup
LOCATION=eastus

# Virtual Network Configuration (VNET and SUBNET names automatically set to VMNAME-VNET and VMNAME-SUBNET)
VNET_ADDRESS_PREFIX=10.0.0.0/16
SUBNET_ADDRESS_PREFIX=10.0.1.0/24

# VM Configuration
VM_NAME=myVM
VM_SIZE=Standard_B2s
# VM_SIZE=Standard_D2s_v3  # Recommended for Windows (2 vCPU, 8GB RAM)
ADMIN_USERNAME=azureuser

# VM Image - Choose operating system
VM_IMAGE=Ubuntu2204
# VM_IMAGE=Win2022Datacenter  # Windows Server 2022
# VM_IMAGE=Ubuntu2004         # Ubuntu 20.04 LTS
# VM_IMAGE=CentOS85Gen2       # CentOS 8.5
# VM_IMAGE=Debian11           # Debian 11
# VM_IMAGE=RHEL85Gen2         # Red Hat Enterprise Linux 8.5

# Public IP Configuration (yes/no) - PUBLIC_IP_NAME automatically set to VMNAME-PIP
CREATE_PUBLIC_IP=yes

# Tag for resource identification (automatically set to VM name)

# Network Security Group (automatically set to VMNAME-NSG)

# Optional: SSH Key Path (leave empty to generate password)
SSH_KEY_PATH=

# Optional: Admin Password for Windows VMs (SECURITY RISK: stored in plain text)
# Leave empty to be prompted during creation (more secure)
# Must be 12-72 chars with 3 of: lowercase, uppercase, numbers, symbols
# Example: SecureAdmin2024!
ADMIN_PASSWORD=